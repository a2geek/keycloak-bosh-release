processes:
- name: keycloak
  executable: /var/vcap/jobs/keycloak/bin/start
  workdir: /var/vcap/store/kc

  additional_volumes:
  - path: /var/vcap/store/kc
    writable: true
    allow_executions: true

  capabilities:
  - NET_BIND_SERVICE
